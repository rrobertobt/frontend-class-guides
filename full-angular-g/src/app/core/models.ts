export type User = {
  id: number;
  email: string;
  name: string;
  createdAt: string;
};

export type Todo = {
  id: number;
  title: string;
  done: boolean;
  created_at: string;
  updated_at: string;
};
